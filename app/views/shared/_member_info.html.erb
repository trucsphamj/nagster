<div class="member_info">
  <a href="<%= member_path(current_member) %>">
    <%= gravatar_for current_member, :size => 30 %>
    <span class="member_name">
      <%= current_member.name %>
    </span>
    <span class="nags">
      <%= pluralize(current_member.nags.count, "nag") %>
    </span>
  </a>
</div>     